name: Like Pizza

on: 
  issue_comment:
    types:
      - created

jobs:
  like-pizza:
    runs-on: ubuntu-latest
      #env:
      #  AUTH_LIST: 'JohnDoe etpalmer63 JaneDoe jmsexton03'
      #    #if: ${{ (github.event.comment.body == 'I like pizza') && contains( "etpalmer63", github.event.comment.user.login) }}
    steps:
      - name: Like Pizza
        if: ${{ (github.event.comment.body == 'I like cats') && contains( secrets.AUTH_LIST, github.event.comment.user.login) }}
        uses: actions/github-script@v4
        with: 
          script:  |
            const {owner, repo} = context.issue
            github.reactions.createForIssueComment({
              owner,
              repo,
              comment_id: context.payload.comment.id,
              content: "+1",
            });
      #- name: trigger job
      #  uses: wei/curl@v1 
      #  with:  
      #    args: -X POST -F token=${{secrets.GITLAB_TRIGGER_TOKEN}} -F ref=main https://software.nersc.gov/api/v4/projects/307/trigger/pipeline
